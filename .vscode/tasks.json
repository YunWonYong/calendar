{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start docker services",
            "type": "shell",
            "command": "./backend/scripts/dev-start.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
        }
    ]
}