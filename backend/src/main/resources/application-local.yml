spring:
    data:
        redis:
            host: localhost
            port: 6379
            lettuce:
                pool:max-active: 10   # 동시에 사용할 수 있는 최대 커넥션 개수
                max-idle: 10     # 풀에서 대기하는 최대 커넥션 개수
                min-idle: 2      # 풀에서 상시 유지하는 최소 커넥션 개수
                max-wait: 2000ms # 커넥션이 꽉 찼을 때 대기하는 최대 시간
    flyway:
        enabled: true
        locations: classpath:db/migration
    datasource:
        url: jdbc:postgresql://localhost:5432/hswy
        username: calendar
        password: Asd!23QwE4%6
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                format_sql: true
front-end:
    base-url: "http://localhost:3001"
    paths:
        oauth2:
            success-path: "/"
            failure-path: "/login"
    access-token:
        jwt-secret: vlzkcbWkdWkdaos!!07AEDWOPakw@!@!
        jwt-expired-seconds: 900
        refresh-expired-seconds: 999999
logging:
    level:
        org.flywaydb: DEBUG
        org.springframework.boot.autoconfigure.flyway: DEBUG