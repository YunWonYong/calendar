{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start docker services",
            "type": "shell",
            "command": "./scripts/dev-start.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
        }
    ]
}